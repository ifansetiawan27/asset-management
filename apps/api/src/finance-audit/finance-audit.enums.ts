/** Enum untuk Module 4 — Depreciation & Audit. */

export enum AuditSessionStatus {
  PLANNED = 'PLANNED',
  IN_PROGRESS = 'IN_PROGRESS',
  CLOSED = 'CLOSED',
}

export enum AuditItemStatus {
  FOUND = 'FOUND',
  MISSING = 'MISSING',
  DAMAGED = 'DAMAGED',
  RELOCATED = 'RELOCATED',
}
